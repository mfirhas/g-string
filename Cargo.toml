[package]
name = "g-string"
version = "0.0.0"
edition = "2024"
description = "Generic String"
license = "MIT"

[package.metadata.docs.rs]
all-features = true

[features]
default = ["std"]
std = ["alloc"]
alloc = []

[dependencies]
